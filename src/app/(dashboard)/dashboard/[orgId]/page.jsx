import ViewOverviewWorspace from '@/features/overview/overview-view';

export default async function OverviewWorspace() {

  //   const session = await auth.api.getSession({
  //     headers: await headers(),
  //   });

  return <ViewOverviewWorspace />;
}
